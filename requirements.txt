# Core dependencies for HDR VAE Decode
# Note: torch, torchvision, numpy are provided by ComfyUI
pillow>=8.0.0
opencv-python>=4.5.0
einops>=0.4.0
pyexr>=0.5.0
openexr>=3.4.0
kornia>=0.8.1

# Optional dependencies for professional VFX workflows
# OpenImageIO is the industry-standard library for HDR/EXR file handling
# Provides true 32-bit/16-bit float precision with full compression support
OpenImageIO>=2.4.0  # Professional VFX-grade EXR support (cross-platform)
