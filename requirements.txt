# Core dependencies for HDR VAE Decode
# Note: torch, torchvision, numpy are provided by ComfyUI
pillow>=8.0.0
opencv-python>=4.5.0
einops>=0.4.0

# Optional dependencies for advanced formats
OpenEXR>=1.3.9; platform_system != "Windows"
Imath>=0.0.2; platform_system != "Windows"

# For Windows EXR support (alternative)
imageio>=2.25.0
imageio-ffmpeg>=0.4.8
